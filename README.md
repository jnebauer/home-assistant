# Home Assistant
Configuration and dashboards for my Home Assistant instance.

## Kiosk Dashboard
Custom wall-panel kiosk in `ha-kiosk/kiosk.html`.

### Deploy after changes
Run `./deploy.sh` on your Mac, then paste the printed curl command into the HA SSH terminal.
